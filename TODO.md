# TODO

- [ ] Add Managing Director welcome message section to `client/src/pages/Home.tsx`.
- [ ] Add an aligned layout: left animated/featured image + right welcome text (or responsive stack on mobile).
- [ ] Ensure typography and alignment are visually consistent with existing Tailwind styles.
- [ ] Use an existing image from `client/public/gallery/` (or current hero slideshow) to display alongside the message.
- [x] Update `client/src/pages/Home.tsx` only (unless styling needs new CSS helpers).
- [x] Run client build/dev command to ensure TypeScript/React compiles.


